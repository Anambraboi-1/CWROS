services:
  api:
    build: ./apps/api
    environment:
      JWT_SECRET: development-only-change-me
      CORS_ORIGIN: http://localhost:5173
      DATABASE_PATH: /app/data/cwros.db
    volumes: ["api-data:/app/data"]
    ports: ["4000:4000"]
volumes:
  api-data:
